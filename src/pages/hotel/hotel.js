import React, { lazy, Suspense } from 'react';
import ReactDom from 'react-dom';

//Componentes
import App from './App';
const estado = '';

ReactDom.render(
  <>
    <App />
  </>,
  document.getElementById('root'),
);
